language: node_js
node_js:
  - "node"
script: 
  - yarn test
  - yarn run docs
  - mkdir public/docs/api/tests
  - cp tests/eslint-report.html public/docs/api/tests/
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: public/docs/api
  github_token: $GITHUB_TOKEN
  on:
    branch: master